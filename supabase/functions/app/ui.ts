export const APP_HTML = String.raw`<!doctype html>
<html lang="ru"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<title>НАСЫПАТЕЛИ В КИНО</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<style>
:root{--bg:#f3efe7;--ink:#161616;--muted:#77736c;--card:#fffdf8;--line:#d9d2c8;--pink:#f2b8cb;--green:#b9f08c;--yellow:#f6da73;--red:#e96a61;--shadow:0 10px 35px rgba(20,18,15,.08);font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--ink);background:var(--bg)}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink)}button,input,textarea,select{font:inherit}button{cursor:pointer}.wrap{max-width:760px;margin:auto;padding:18px 16px 100px}.brand{font-weight:900;letter-spacing:-.04em;font-size:22px}.hero{padding:38px 0 18px}.hero h1,.screen h1{font-size:clamp(42px,12vw,76px);line-height:.91;letter-spacing:-.075em;margin:8px 0 20px}.hero p{font-size:18px;line-height:1.4;max-width:600px}.eyebrow{text-transform:uppercase;font-size:11px;font-weight:900;letter-spacing:.12em;color:var(--muted)}.card{background:var(--card);border:1px solid var(--line);border-radius:20px;padding:18px;margin:12px 0;box-shadow:var(--shadow)}.row{display:flex;gap:10px;align-items:center}.spread{justify-content:space-between}.stack{display:grid;gap:10px}.muted{color:var(--muted)}.pill{display:inline-flex;border:1px solid var(--line);border-radius:999px;padding:6px 10px;font-size:12px;font-weight:800}.btn{width:100%;border:0;border-radius:14px;padding:14px 16px;margin-top:10px;background:var(--ink);color:white;font-weight:800}.btn.secondary{background:transparent;color:var(--ink);border:1px solid var(--line)}.btn.good{background:#377a36}.btn.danger{background:#a3322e}.btn:disabled{opacity:.35;cursor:not-allowed}.section{font-size:13px;text-transform:uppercase;letter-spacing:.08em;font-weight:900;margin-bottom:12px}.big{font-size:28px;line-height:1.05;font-weight:900;letter-spacing:-.04em}input,textarea,select{width:100%;background:white;border:1px solid var(--line);border-radius:12px;padding:12px;color:var(--ink)}textarea{min-height:96px;resize:vertical}.field{margin:12px 0}.field label{display:block;font-size:12px;font-weight:800;margin-bottom:6px}.check{display:flex;gap:10px;align-items:flex-start;margin:15px 0}.check input{width:auto;margin-top:3px}.notice{padding:12px;border-radius:12px;background:#effbe7;border:1px solid #b8df9d;margin:10px 0}.error{padding:12px;border-radius:12px;background:#fff0ee;border:1px solid #eab4ae;margin:10px 0;color:#8b2d27}.meter{height:8px;background:#e7e0d8;border-radius:99px;overflow:hidden;margin:13px 0}.meter i{display:block;height:100%;background:var(--ink)}.nav{position:fixed;bottom:0;left:0;right:0;background:rgba(243,239,231,.94);backdrop-filter:blur(14px);border-top:1px solid var(--line);display:flex;justify-content:center;z-index:20}.nav-inner{width:min(760px,100%);display:grid;grid-template-columns:repeat(4,1fr);padding:7px 8px calc(7px + env(safe-area-inset-bottom))}.nav button{border:0;background:none;padding:10px 4px;font-size:12px;color:var(--muted);font-weight:750}.nav button.active{color:var(--ink)}.idea{padding:12px 0;border-top:1px solid var(--line)}.idea:first-child{border:0}.idea h3{margin:0 0 5px}.binary{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}.binary button,.rating button{border:1px solid var(--line);background:white;border-radius:10px;padding:9px}.binary button.active,.rating button.active{background:var(--ink);color:white}.prediction{padding:13px 0;border-top:1px solid var(--line)}.rating{display:grid;grid-template-columns:repeat(5,1fr);gap:6px}.leader{display:grid;grid-template-columns:28px 1fr auto;gap:8px;padding:10px 0;border-top:1px solid var(--line);align-items:center}.leader:first-child{border:0}.admin{max-width:1200px;margin:auto;padding:20px}.admin-grid{display:grid;grid-template-columns:minmax(280px,.75fr) minmax(320px,1.25fr);gap:14px}.stage-list{display:grid;gap:6px}.stage{border:1px solid var(--line);background:white;border-radius:12px;padding:10px;text-align:left}.stage.current{background:var(--ink);color:white}.stage.done{opacity:.55}.output{white-space:pre-wrap;overflow:auto;background:#181818;color:#f5f0e6;border-radius:12px;padding:12px;font-size:11px;max-height:360px}.screen{min-height:100vh;background:#111;color:#f7f0e7;padding:4vw;display:flex;flex-direction:column}.screen-top{display:flex;justify-content:space-between;gap:20px;text-transform:uppercase;letter-spacing:.1em;font-size:1.2vw;font-weight:800;opacity:.7}.screen-main{flex:1;display:flex;flex-direction:column;justify-content:center;align-items:flex-start;max-width:90vw}.screen h1{font-size:clamp(60px,9vw,150px);max-width:1200px}.screen h2{font-size:clamp(28px,4vw,70px);margin:12px 0}.screen p{font-size:clamp(20px,2.2vw,42px);max-width:1200px}.screen-message{position:fixed;right:4vw;bottom:4vw;background:var(--pink);color:#111;padding:14px 18px;border-radius:16px;font-size:2vw;font-weight:900}.screen .list{font-size:clamp(25px,3.3vw,54px);font-weight:800;display:grid;gap:14px}.scan{animation:pulse 1.2s infinite alternate}@keyframes pulse{to{opacity:.35}}@media(max-width:800px){.admin-grid{grid-template-columns:1fr}.admin{padding:12px}.hero{padding-top:24px}}
@media(max-width:430px){.wrap{padding:14px 12px calc(94px + env(safe-area-inset-bottom));max-width:100%;overflow-x:hidden}.card{padding:15px}.row{min-width:0;flex-wrap:wrap}.spread{align-items:flex-start}.hero h1{font-size:clamp(38px,13vw,56px);overflow-wrap:anywhere}.big{font-size:25px;overflow-wrap:anywhere}.binary{grid-template-columns:minmax(0,1fr) minmax(0,1fr)}.nav-inner{grid-template-columns:repeat(4,minmax(0,1fr))}.nav button{min-width:0;font-size:11px}.field,input,textarea,select,.card{min-width:0;max-width:100%}.output{word-break:break-word}.admin{max-width:100%;overflow-x:hidden}}
@media(max-width:340px){.wrap{padding-left:10px;padding-right:10px}.card{padding:13px}.hero h1{font-size:38px}.binary{grid-template-columns:1fr}}
</style></head><body><div id="app"></div>
<script>
(()=>{
const API=location.origin+location.pathname, SLUG=new URLSearchParams(location.search).get('slug')||'2026-10-03';
const view=new URLSearchParams(location.search).get('view')||'participant';
const hash=new URLSearchParams(location.hash.replace(/^#/,''));
if(view==='admin'&&hash.get('key')){sessionStorage.setItem('nasyp-admin-key',hash.get('key'));history.replaceState({},'',location.pathname+'?view=admin&slug='+encodeURIComponent(SLUG))}
if(view==='screen'&&hash.get('key')){sessionStorage.setItem('nasyp-screen-key',hash.get('key'));history.replaceState({},'',location.pathname+'?view=screen&slug='+encodeURIComponent(SLUG))}
const tg=window.Telegram?.WebApp;tg?.ready?.();tg?.expand?.();
let data=null,error='',tab='home',busy=false,drafts={},predAnswers={},actuals={};
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const labels={DRAFT:'черновик',SALES_OPEN:'продажа билетов',CHECKIN:'сбор гостей',IDEAS_OPEN:'заявки открыты',IDEAS_LOCKED:'заявки закрыты',TOP3_READY:'три идеи выбраны',IDEA_RANDOMIZED:'идея выбрана',MOVIE_SEARCH:'поиск фильма',MOVIE_FINALISTS:'три фильма',MOVIE_SELECTED:'фильм выбран',PREDICTIONS_OPEN:'прогнозы открыты',PREDICTIONS_LOCKED:'прогнозы закрыты',WATCHING:'просмотр',PREDICTIONS_SCORED:'результаты',DISCUSSION:'обсуждение',FINAL_REVIEW:'финальная рецензия',FEEDBACK:'обратная связь',CLOSED:'закрыто'};
const stages=Object.keys(labels);const manualNext={DRAFT:'SALES_OPEN',SALES_OPEN:'CHECKIN',CHECKIN:'IDEAS_OPEN',IDEAS_OPEN:'IDEAS_LOCKED',PREDICTIONS_OPEN:'PREDICTIONS_LOCKED',PREDICTIONS_LOCKED:'WATCHING',PREDICTIONS_SCORED:'DISCUSSION',DISCUSSION:'FINAL_REVIEW',FINAL_REVIEW:'FEEDBACK',FEEDBACK:'CLOSED'};
function headers(){const h={'content-type':'application/json'};if(view==='admin')h['x-admin-token']=sessionStorage.getItem('nasyp-admin-key')||'';else if(view==='screen')h['x-screen-token']=sessionStorage.getItem('nasyp-screen-key')||'';else h['x-telegram-init-data']=tg?.initData||'';return h}
async function api(action,payload={}){const r=await fetch(API,{method:'POST',headers:headers(),body:JSON.stringify({action,slug:SLUG,...payload})});const j=await r.json().catch(()=>({error:'invalid response'}));if(!r.ok)throw new Error(j.error||('HTTP '+r.status));return j}
async function bootstrap(){try{error='';const a=view==='admin'?'admin-bootstrap':view==='screen'?'screen-bootstrap':'bootstrap';data=await api(a);if(data?.profile&&!drafts.profile)drafts.profile={...data.profile};render()}catch(e){error=e.message||String(e);render()}}
async function run(action,payload={}){if(busy)return;busy=true;render();try{await api(action,payload);await bootstrap()}catch(e){error=e.message||String(e);render()}finally{busy=false;render()}}
function btn(text,onclick,cls='',disabled=false){return '<button class="btn '+cls+'" '+(disabled?'disabled':'')+' data-act="'+onclick+'">'+esc(text)+'</button>'}
function card(x){return '<div class="card">'+x+'</div>'}
function bind(){document.querySelectorAll('[data-act]').forEach(b=>b.onclick=actions[b.dataset.act]);document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()})}
const actions={};
function participantShell(content){return '<div class="wrap"><div class="brand">НАСЫПАТЕЛИ В КИНО</div>'+ (error?'<div class="error">'+esc(error)+'</div>':'') +content+'</div><div class="nav"><div class="nav-inner">'+[['home','главная'],['profile','профиль'],['event','вечер'],['club','клуб']].map(x=>'<button data-tab="'+x[0]+'" class="'+(tab===x[0]?'active':'')+'">'+x[1]+'</button>').join('')+'</div></div>'}
function renderParticipant(){if(!data)return participantShell(card('загрузка…'));if(!data.event)return participantShell(card('пока нет ближайшего события'));const e=data.event;
if(tab==='profile')return participantShell(profileView());if(tab==='event')return participantShell(eventView());if(tab==='club')return participantShell(clubView());
const left=Math.max(0,e.capacity-e.sold);let ticket='';if(data.registration==='none')ticket=btn(left?'купить билет':'встать в лист ожидания','buy');else ticket='<div class="notice">'+(data.registration==='paid'?'билет ваш. вы внутри':data.registration==='reserved'?'место зарезервировано на 15 минут':'вы в листе ожидания')+'</div>';
return participantShell('<div class="hero"><div class="eyebrow">первый тестовый вечер</div><h1>никто не знает,<br>что мы посмотрим</h1><p>вы придумываете фильм, которого не существует. животина ищет ему реального двойника. рандом решает остальное</p></div>'+card('<div class="row spread"><b>3 октября · москва</b><span class="pill">'+e.ticketPriceRub+' ₽</span></div><div class="meter"><i style="width:'+Math.min(100,e.sold/e.capacity*100)+'%"></i></div><div class="muted">'+e.sold+' из '+e.capacity+' мест · осталось '+left+'</div>'+ticket)+card('<div class="section">как это работает</div><div class="stack"><div>01 · придумать несуществующий фильм</div><div>02 · животина выберет три</div><div>03 · рандом выберет одну идею</div><div>04 · найдём реальный фильм-двойник</div><div>05 · сделаем десять прогнозов</div><div>06 · посмотрим, поспорим и напишем общую рецензию</div></div>'))}
actions.buy=async()=>{try{busy=true;render();const r=await fetch(API+'?mode=invoice',{method:'POST',headers:headers(),body:JSON.stringify({slug:SLUG})});const j=await r.json();if(r.status===409&&j.waitlist){await bootstrap();return}if(!r.ok)throw new Error(j.error||'payment error');if(j.alreadyPaid){await bootstrap();return}if(j.invoiceUrl){if(tg?.openInvoice)tg.openInvoice(j.invoiceUrl,()=>setTimeout(bootstrap,1000));else location.href=j.invoiceUrl}}catch(e){error=e.message;render()}finally{busy=false}};
function profileView(){const p=drafts.profile||data.profile||{};return '<h2>ваш кинопрофиль</h2><p class="muted">животина учитывает его при подборе фильма</p>'+card('<div class="field"><label>как вас называть</label><input id="p-name" value="'+esc(p.displayName||'')+'"></div><div class="field"><label>любимые фильмы · через запятую</label><textarea id="p-films">'+esc(p.favoriteFilms||'')+'</textarea></div><div class="field"><label>любимые жанры</label><input id="p-genres" value="'+esc(p.favoriteGenres||'')+'"></div><div class="field"><label>что совсем не хочется смотреть</label><textarea id="p-avoid">'+esc(p.avoid||'')+'</textarea></div><label class="check"><input id="p-consent" type="checkbox" '+(p.photoVideoConsent?'checked':'')+'><span>можно снимать меня и использовать фото/видео с мероприятия</span></label>'+btn('сохранить профиль','saveProfile'))}
actions.saveProfile=async()=>{drafts.profile={displayName:$('#p-name').value,favoriteFilms:$('#p-films').value,favoriteGenres:$('#p-genres').value,avoid:$('#p-avoid').value,photoVideoConsent:$('#p-consent').checked};await run('save-profile',{profile:drafts.profile})};
function finalists(title,items){return card('<div class="section">'+esc(title)+'</div>'+items.map((x,i)=>'<div class="idea"><h3>'+(i+1)+'. '+esc(x.title||x.name)+'</h3><div class="muted">'+esc(x.plot||x.reason||'')+'</div></div>').join(''))}
function eventView(){const s=data.event.status;let out='<div class="row spread"><h2>3 октября</h2><span class="pill">'+esc(labels[s]||s)+'</span></div>';
if(['SALES_OPEN','CHECKIN','DRAFT'].includes(s))out+=card('<div class="big">пока собираемся</div><p>заполните профиль. заявка откроется перед началом</p>');
else if(s==='IDEAS_OPEN')out+=card('<div class="section">придумайте фильм, которого не существует</div><div class="field"><label>название · до 5–7 слов</label><input id="idea-title" maxlength="100" value="'+esc(drafts.ideaTitle||'')+'"></div><div class="field"><label>сюжет · до 500 знаков</label><textarea id="idea-plot" maxlength="500">'+esc(drafts.ideaPlot||'')+'</textarea></div>'+btn(data.idea?'обновить заявку':'отправить анонимно','submitIdea')+(data.idea?'<div class="notice">заявка уже принята</div>':''));
else if(['IDEAS_LOCKED','TOP3_READY'].includes(s))out+=finalists('три идеи животины',data.ideaFinalists||[]);
else if(['IDEA_RANDOMIZED','MOVIE_SEARCH'].includes(s))out+=card('<div class="eyebrow">выбрано рандомом</div><div class="big">'+esc(data.selectedIdea?.title||'')+'</div><p>'+esc(data.selectedIdea?.plot||'')+'</p>'+(data.selectedIdea?.author?'<p class="muted">автор: '+esc(data.selectedIdea.author)+'</p>':'')+'<div class="scan">животина ищет реального двойника…</div>');
else if(s==='MOVIE_FINALISTS')out+=finalists('три реальных двойника',(data.movieFinalists||[]).map(x=>({title:x.title+' · '+(x.runtimeMin||'?')+' мин',reason:x.reason})));
else if(s==='MOVIE_SELECTED')out+=movieCard();
else if(s==='PREDICTIONS_OPEN')out+=predictionsView();
else if(['PREDICTIONS_LOCKED','WATCHING'].includes(s))out+=card('<div class="big">телефоны вниз</div><p>'+(data.predictionSubmitted?'ваши прогнозы зафиксированы. ':'')+'смотрим фильм целиком</p>');
else if(s==='PREDICTIONS_SCORED')out+=card('<div class="big">прогнозы посчитаны</div><p>результаты на общем экране</p>');
else if(s==='DISCUSSION')out+=card('<div class="section">одна мысль животине</div><textarea id="thought" maxlength="240">'+esc(drafts.thought??data.thought??'')+'</textarea>'+btn('отправить','submitThought'));
else if(s==='FINAL_REVIEW')out+=reviewView();
else if(s==='FEEDBACK'||s==='CLOSED')out+=feedbackView(s==='CLOSED');
return out}
function movieCard(){const m=data.selectedMovie||{};return card('<div class="eyebrow">фильм вечера</div><div class="big">'+esc(m.title||'')+'</div><p>'+esc(m.year||'')+' · '+esc(m.runtimeMin||'?')+' минут</p><p class="muted">'+esc(m.reason||'')+'</p>')}
actions.submitIdea=async()=>{drafts.ideaTitle=$('#idea-title').value;drafts.ideaPlot=$('#idea-plot').value;await run('submit-idea',{title:drafts.ideaTitle,plot:drafts.ideaPlot})};
function predictionsView(){return card('<div class="section">10 прогнозов</div><p class="muted">что будет в фильме? после старта ответы не изменить</p>'+(data.predictions||[]).map(p=>'<div class="prediction"><b>'+p.position+'. '+esc(p.text)+'</b><div class="binary"><button data-pid="'+p.id+'" data-v="1" class="'+(predAnswers[p.id]===true?'active':'')+'">будет</button><button data-pid="'+p.id+'" data-v="0" class="'+(predAnswers[p.id]===false?'active':'')+'">не будет</button></div></div>').join('')+btn(data.predictionSubmitted?'ответы зафиксированы':'зафиксировать прогнозы','submitPred','',data.predictionSubmitted))}
actions.submitPred=async()=>{const qs=data.predictions||[];if(Object.keys(predAnswers).length!==qs.length){error='ответьте на все 10 прогнозов';render();return}await run('submit-predictions',{predictions:qs.map(p=>({...p,answer:predAnswers[p.id]}))})};
actions.submitThought=async()=>{drafts.thought=$('#thought').value;await run('submit-thought',{text:drafts.thought})};
function reviewView(){return card('<div class="section">ваша строка в общей рецензии</div><div class="field"><label>оценка 1–10</label><input id="rating" type="number" min="1" max="10" value="'+esc(drafts.rating??data.review?.rating??7)+'"></div><div class="field"><label>ровно одна финальная фраза</label><textarea id="sentence" maxlength="180">'+esc(drafts.sentence??data.review?.sentence??'')+'</textarea></div>'+btn('сохранить без редактуры','submitReview'))}
actions.submitReview=async()=>{drafts.rating=Number($('#rating').value);drafts.sentence=$('#sentence').value;await run('submit-review',{rating:drafts.rating,sentence:drafts.sentence})};
function feedbackView(closed){const f=data.feedback||{};return card('<div class="section">'+(closed?'ваш итоговый ответ':'это был тест')+'</div><div class="field"><label>хотите прийти снова?</label><select id="return"><option>да</option><option>скорее да</option><option>скорее нет</option><option>нет</option></select></div><div class="field"><label>что сработало лучше всего?</label><textarea id="strong">'+esc(f.strongest||'')+'</textarea></div><div class="field"><label>что нужно изменить?</label><textarea id="improve">'+esc(f.improve||'')+'</textarea></div><div class="field"><label>как ощущалась длительность?</label><select id="duration"><option>коротко</option><option selected>нормально</option><option>долго</option></select></div><div class="field"><label>насколько вероятно, что позовёте друга? 0–10</label><input id="invite" type="number" min="0" max="10" value="'+esc(f.inviteFriend??8)+'"></div><div class="field"><label>сколько готовы платить за следующий офлайн, ₽?</label><input id="wtp" type="number" min="0" value="'+esc(f.willingness||1000)+'"></div>'+btn('отправить','submitFeedback'))}
actions.submitFeedback=async()=>{await run('submit-feedback',{feedback:{returnIntent:$('#return').value,strongest:$('#strong').value,improve:$('#improve').value,durationFeel:$('#duration').value,inviteFriend:Number($('#invite').value),willingness:Number($('#wtp').value)}})};
function clubView(){return '<h2>клуб</h2>'+card('<div class="section">таблица прогнозистов</div>'+((data.leaderboard||[]).length?(data.leaderboard||[]).map((x,i)=>'<div class="leader"><span>'+(i+1)+'</span><b>'+esc(x.name)+'</b><strong>'+x.points+'</strong></div>').join(''):'<p class="muted">рейтинг появится после первого вечера</p>'))+card('<div class="section">дальше</div><p>история показов, оценки, артефакты, знакомства, секретный билет и разговоры с животиной о других фильмах</p>')}
function renderAdmin(){if(!sessionStorage.getItem('nasyp-admin-key'))return document.getElementById('app').innerHTML='<div class="admin">'+card('<div class="big">нужен ключ админки</div><p class="muted">откройте ссылку вида <code>?view=admin#key=…</code>. ключ сохранится только в этой вкладке</p>')+'</div>';if(!data)return document.getElementById('app').innerHTML='<div class="admin">'+card(error?'<div class="error">'+esc(error)+'</div>':'загрузка…')+'</div>';const e=data.event,idx=stages.indexOf(e.status);let auto='';
const map={IDEAS_LOCKED:['ai-select-ideas','животина: выбрать топ-3'],TOP3_READY:['draw-idea','запустить рандом идеи'],IDEA_RANDOMIZED:['ai-find-movies','найти и проверить фильмы'],MOVIE_SEARCH:['ai-find-movies','повторить поиск фильмов'],MOVIE_FINALISTS:['draw-movie','запустить рандом фильма'],MOVIE_SELECTED:['ai-generate-predictions','сгенерировать 10 прогнозов'],DISCUSSION:['ai-post-film-synthesis','животина: собрать разбор группы'],FINAL_REVIEW:['ai-collective-review','собрать черновик общей рецензии']};if(map[e.status])auto=btn(map[e.status][1],'autoAction');
if(['PREDICTIONS_LOCKED','WATCHING'].includes(e.status))auto=factCheck();
if(e.status==='PREDICTIONS_SCORED')auto=winnerAdmin();
if(e.status==='CLOSED')auto=btn('посчитать исследование','research')+btn('сохранить память животины','memory','secondary')+(data.outputs?.research_summary?'<pre class="output">'+esc(JSON.stringify(data.outputs.research_summary,null,2))+'</pre>':'');
const html='<div class="admin"><div class="row spread"><div><div class="eyebrow">режиссёрский пульт</div><h1>'+esc(e.title)+'</h1></div><span class="pill">'+esc(labels[e.status]||e.status)+'</span></div>'+(error?'<div class="error">'+esc(error)+'</div>':'')+'<div class="admin-grid">'+card('<div class="section">этап вечера</div><div class="stage-list">'+stages.map((s,i)=>'<div class="stage '+(s===e.status?'current':i<idx?'done':'')+'">'+String(i+1).padStart(2,'0')+' · '+esc(labels[s])+'</div>').join('')+'</div>')+'<div>'+card('<div class="section">быстро</div>'+(manualNext[e.status]?btn('следующий этап →','nextStage'):'<p class="muted">следующий этап откроется после действия ниже</p>'))+card('<div class="section">Telegram</div>'+btn('подключить webhook + кнопку меню','configureTelegram','secondary'))+card('<div class="section">тестовые билеты</div><div class="field"><input id="testuser" placeholder="@username · сначала человек должен открыть бота"></div>'+btn('выдать бесплатный тестовый билет','grantTest','secondary'))+card('<div class="section">вместимость</div><div class="field"><input id="capacity" type="number" value="'+e.capacity+'"></div>'+btn('применить','capacity')+'<p class="muted">продано '+e.sold+'</p>')+card('<div class="section">сообщение на экран</div><textarea id="screenmsg" placeholder="10 минут до начала">'+esc(data.screenMessage||'')+'</textarea>'+btn('показать','screenMessage'))+card('<div class="section">автоматизация</div>'+auto)+(Object.keys(data.outputs||{}).length?card('<div class="section">результаты системы</div><pre class="output">'+esc(JSON.stringify(data.outputs,null,2))+'</pre>'):'')+'</div></div></div>';document.getElementById('app').innerHTML=html;bindAdmin(map)}
function bindAdmin(map){const s=data.event.status;actions.nextStage=()=>manualNext[s]&&run('admin-stage',{status:manualNext[s]});actions.capacity=()=>run('admin-capacity',{capacity:Number($('#capacity').value)});actions.screenMessage=()=>run('admin-screen-message',{message:$('#screenmsg').value});actions.autoAction=()=>{const m=map[s];if(m)run(m[0])};actions.research=()=>run('admin-research-summary');actions.memory=()=>run('ai-finalize-memory');actions.configureTelegram=()=>run('admin-configure-telegram');actions.grantTest=()=>run('admin-grant-test-ticket',{username:$('#testuser').value});bind();document.querySelectorAll('[data-actual]').forEach(b=>b.onclick=()=>{actuals[b.dataset.actual]=b.dataset.v==='void'?'void':b.dataset.v==='1';render()});document.querySelectorAll('[data-winner]').forEach(b=>b.onclick=()=>run('admin-set-winner',{userId:b.dataset.winner}))}
function factCheck(){return '<div class="section">факт-чек прогнозов</div>'+(data.predictions||[]).map(p=>'<div class="prediction"><b>'+p.position+'. '+esc(p.text)+'</b><div class="binary" style="grid-template-columns:repeat(3,1fr)"><button data-actual="'+p.id+'" data-v="1" class="'+(actuals[p.id]===true?'active':'')+'">было</button><button data-actual="'+p.id+'" data-v="0" class="'+(actuals[p.id]===false?'active':'')+'">не было</button><button data-actual="'+p.id+'" data-v="void" class="'+(actuals[p.id]==='void'?'active':'')+'">не считаем</button></div></div>').join('')+btn('посчитать победителя','score','',Object.keys(actuals).length!==(data.predictions||[]).length)}
actions.score=()=>run('admin-score-predictions',{actuals});
function winnerAdmin(){const s=data.outputs?.score_summary,t=data.outputs?.tiebreaker;if(!s)return '<p class="muted">нет сохранённого подсчёта</p>';if(s.winner&&!s.tie)return '<div class="notice">победитель: '+esc(s.winner.name)+'</div>';return '<p>ничья: '+esc((s.winners||[]).map(x=>x.name).join(', '))+'</p>'+btn(t?'сгенерировать ещё убогий пересказ':'животина: тай-брейкер','tiebreaker')+(t?.clue?'<div class="card"><b>'+esc(t.clue)+'</b></div><p class="muted">кто первым угадал?</p>'+(s.winners||[]).map(w=>'<button class="btn secondary" data-winner="'+esc(w.userId)+'">'+esc(w.name)+'</button>').join(''):'')}
actions.tiebreaker=()=>run('ai-tiebreaker');
function screenMain(){const s=data.event.status;let x='';if(['DRAFT','SALES_OPEN','CHECKIN'].includes(s))x='<h1>этого фильма<br>не существует</h1><p>'+data.event.sold+'/'+data.event.capacity+' внутри</p>';else if(s==='IDEAS_OPEN')x='<h1>придумайте<br>несуществующий фильм</h1><p>название + короткий сюжет</p>';else if(['IDEAS_LOCKED','TOP3_READY'].includes(s))x='<h1>животина выбрала три</h1><div class="list">'+(data.ideaFinalists||[]).map((a,i)=>(i+1)+'. '+esc(a.title)).join('<br>')+'</div>';else if(['IDEA_RANDOMIZED','MOVIE_SEARCH'].includes(s))x='<div class="eyebrow scan">поиск реального двойника</div><h1>'+esc(data.selectedIdea?.title||'')+'</h1>'+(data.selectedIdea?.author?'<p>автор: '+esc(data.selectedIdea.author)+'</p>':'');else if(s==='MOVIE_FINALISTS')x='<h1>финальная тройка</h1><div class="list">'+(data.movieFinalists||[]).map((a,i)=>(i+1)+'. '+esc(a.title)+' · '+a.runtimeMin+' мин').join('<br>')+'</div>';else if(['MOVIE_SELECTED','PREDICTIONS_OPEN','PREDICTIONS_LOCKED'].includes(s))x='<div class="eyebrow">сегодня смотрим</div><h1>'+esc(data.selectedMovie?.title||'')+'</h1><p>'+esc(data.selectedMovie?.year||'')+' · '+esc(data.selectedMovie?.runtimeMin||'')+' минут</p>';else if(s==='WATCHING')x='<h1>смотрим</h1><p>телефоны вниз</p>';else if(s==='PREDICTIONS_SCORED'){const q=data.outputs?.score_summary,t=data.outputs?.tiebreaker;if(q?.winner&&!q?.tie)x='<div class="eyebrow">победитель прогнозов</div><h1>'+esc(q.winner.name)+'</h1>';else if(q?.tie&&t?.clue)x='<div class="eyebrow">тай-брейкер</div><h2>'+esc(t.clue)+'</h2><p>кто первым назовёт фильм?</p>';else x='<h1>кто угадал?</h1><div class="list">'+((q?.scores||data.leaderboard||[]).slice(0,3).map((r,i)=>(r.rank||i+1)+'. '+esc(r.name)+' — '+r.points).join('<br>'))+'</div>'}else if(s==='DISCUSSION'){const o=data.outputs?.post_film_synthesis;x=o?'<div class="eyebrow">животина</div><h2>'+esc(o.consensus)+'</h2><p>'+esc(o.jipitinaTake)+'</p>':'<h1>что вы<br>только что увидели?</h1><p>животина читает ваши мысли</p>'}else if(s==='FINAL_REVIEW')x='<h1>одна фраза.<br>без редактуры</h1>';else if(s==='FEEDBACK')x='<h1>это был тест</h1><p>помогите решить, делать ли второй</p>';else x='<h1>конец первого вечера</h1><p>рецензия — завтра</p>';return x}
function renderScreen(){if(!sessionStorage.getItem('nasyp-screen-key'))return document.getElementById('app').innerHTML='<div class="screen"><div class="screen-main"><h1>нужен ключ экрана</h1><p>откройте ссылку вида ?view=screen#key=…</p></div></div>';if(!data)return document.getElementById('app').innerHTML='<div class="screen"><div class="screen-main"><h1>'+(error?esc(error):'загрузка…')+'</h1></div></div>';document.getElementById('app').innerHTML='<div class="screen"><div class="screen-top"><span>НАСЫПАТЕЛИ В КИНО</span><span>'+esc(labels[data.event.status]||data.event.status)+'</span></div><div class="screen-main">'+screenMain()+'</div>'+(data.screenMessage?'<div class="screen-message">'+esc(data.screenMessage)+'</div>':'')+'</div>'}
function render(){if(view==='admin'){renderAdmin();return}if(view==='screen'){renderScreen();return}document.getElementById('app').innerHTML=renderParticipant()||'';bind();document.querySelectorAll('[data-pid]').forEach(b=>b.onclick=()=>{predAnswers[b.dataset.pid]=b.dataset.v==='1';render()});}
bootstrap();setInterval(()=>{if(!busy&&!document.querySelector('input:focus,textarea:focus,select:focus'))bootstrap()},view==='screen'?2000:view==='admin'?3000:8000);
})();
</script></body></html>`
